# Procfile — tells Render (and other platforms) how to start the app.
# gunicorn is a production-grade web server for Python.
# "app:app" means: find the Flask object named "app" inside the file "app.py"
web: gunicorn app:app
